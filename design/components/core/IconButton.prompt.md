Circular icon-only button for utility actions overlaid on media (back, favorite, share on the product carousel).

```jsx
<IconButton icon={<HeartIcon/>} ariaLabel="Save" tone="light" />
```

Tones: `light` (translucent white, for overlaying photos), `dark` (solid black), `outline` (bordered, on light surfaces).
