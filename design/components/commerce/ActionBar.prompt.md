Fixed bottom bar pinned to the thumb zone. Secondary action (black, ~42% width) sits left of the primary lime CTA (flex-grow), matching the AR View / Add to Cart pairing on the PDP.

```jsx
<ActionBar secondaryLabel="AR View" primaryLabel="Add to Cart" onPrimary={addToCart} />
```
